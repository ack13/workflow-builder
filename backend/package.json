{
  "name": "workflow-builder-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:migrate": "node -r dotenv/config -e \"const url = new URL(process.env.DATABASE_URL); url.searchParams.delete('uselibpqcompat'); require('child_process').execFileSync('psql', [url.toString(), '-f', 'src/db/schema.sql'], { stdio: 'inherit' })\"",
    "db:migrate:delay": "node -r dotenv/config -e \"const url = new URL(process.env.DATABASE_URL); url.searchParams.delete('uselibpqcompat'); require('child_process').execFileSync('psql', [url.toString(), '-f', 'src/db/migrations/001_delay_job_retries.sql'], { stdio: 'inherit' })\"",
    "db:migrate:names": "node -r dotenv/config -e \"const url = new URL(process.env.DATABASE_URL); url.searchParams.delete('uselibpqcompat'); require('child_process').execFileSync('psql', [url.toString(), '-f', 'src/db/migrations/002_unique_workflow_names.sql'], { stdio: 'inherit' })\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.6",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
